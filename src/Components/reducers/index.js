/*
 * Redux reducers
 */

import { combineReducers } from 'redux'

import { initialState } from './store'
import {
    FOLLOW_USER,
    REPLY_THREAD,
    COMMENT_PROFILE,
    FOLLOW_USER,
    REPLY_THREAD,
    COMMENT_PROFILE,
    TOGGLE_SITEMENU,
    LOAD_POST,
    EDIT_POST,
    REMOVE_POST
} from './root-actions'

// TODO
